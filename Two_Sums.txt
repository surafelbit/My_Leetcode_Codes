class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        
        """
        sum =0
        result=[]
        for i in range (len(nums)):
          for j in range (i):
             
            if nums[i]+ nums[j]== target:
              result.append(i)
              result.append(j) 
              return result
              


        