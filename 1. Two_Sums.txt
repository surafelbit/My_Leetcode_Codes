class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        
        """
        sum =0
        result=[]
        for i in range (len(nums)):
          for j in range (i):
             
            if nums[i]+ nums[j]== target:
              result.append(i)
              result.append(j) 
              return result
       
///////////////////// javascript solving with O(n) hash map  //


/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
  let map ={}
for(let i=0;i<nums.length; i++){
    let needed = target - nums[i]
  if(map[needed]!=undefined){
      return [map[needed],i]
    }
    map[nums[i]]= i
}
};       


        