//////////////// javascript ///////////////////////

/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function(s, t) {
    let mapA={}
    let mapB={}
    let count = 1
    
    for (let i =0 ; i<s.length; i++){
        if(mapA[s[i]]!==undefined){
          mapA[s[i]] =  mapA[s[i]] + 1
        }
        else{
            mapA[s[i]] = 1
        }
    }
    for (let j=0; j<t.length;j++){
         if(mapB[t[j]]!==undefined){
          mapB[t[j]] =  mapB[t[j]] + 1
        }
        else{
            mapB[t[j]] = 1
        }
    }
    for (let key in mapA){
        if(mapA[key]!=mapB[key] || ){
            return false
        }
    }
    return true
};




/////////////////////// javascript unrecommeded and messy way solving with sorting ////////////


/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isAnagram = function(s, t) {
     let u = s.split("")
    u.sort()
    k= u.join("")
        let v = s.split("")
     let p = t.split("")
      if (s.length !== t.length) return false;
    for (let i = 1; i<v.length; i++){
     let j = i - 1
     let current = v[i]
 
     while (j>=0 && current<v[j]){
        v[j+1]= v[j]
        j--
     }
     v[j+1]=current
    }
    for (let i =0 ; i <p.length - 1; i++){
        for (let j=0 ; j<p.length-1; j++){
            if(p[j]>p[j+1]){
                [p[j],p[j+1]]=[p[j+1],p[j]]
            }
        }
    }
    for (let i =0; i<p.length; i++){
        if(p[i]!=v[i]){
            return false
        }
    }
    return true