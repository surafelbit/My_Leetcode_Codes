////////////// javascript ////////////////

/**
 * @param {string} s
 * @return {number}
 */
var firstUniqChar = function(s) {
    let map={}
    for (let i=0;i<s.length;i++){
   map[s[i]]= (map[s[i]]||0)+1
    }
    for(key in map){
        if(map[key]==1){
            for(let i=0;i<s.length;i++){
                if(s[i]==key){
                    return i
                }
            }
        }
    }
    return -1
};